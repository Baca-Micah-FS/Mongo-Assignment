const successfulGeoMessage = "verb to GeoData API succeeded";

const geoNotFound = "GeoData not found";

const badRequest = "Bad request";

const apiError = "Upstream API error";

module.exports = {
  successfulGeoMessage,
  geoNotFound,
  badRequest,
  apiError,
};

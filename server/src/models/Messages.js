const notFound = "Object was not found";

const successfulDirectorMessage = "Verb succeeded in Directors Route";

const successfulMovieMessage = "Verb succeeded in Movies Route";

const invalidDirectorId = "Invalid director Id";

const invalidMovieId = "Invalid movie Id";

const directorNotFound = "Director not found";

const movieNotFound = "Movie not found";

const badRequest = "Bad request";

module.exports = {
  notFound,
  successfulDirectorMessage,
  successfulMovieMessage,
  invalidDirectorId,
  invalidMovieId,
  directorNotFound,
  movieNotFound,
  badRequest,
};
